#include <iostream>
#include <vector>
#include <string>
using namespace std;
#define rep(i, a, b) for(i = a; i < b; i++)
using ll = long long;

int main()
{
    int a = 0, b = 0, c = 0;
    string s;

    cin >> a >> b >> c >> s;

    cout << a + b + c << " " << s << "\n";

    return 0;
}